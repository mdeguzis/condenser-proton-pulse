{
  "name": "condenser-proton-pulse",
  "version": "0.1.0",
  "description": "Condenser port of the decky-proton-pulse plugin.",
  "repository": "github:mdeguzis/condenser-proton-pulse",
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "node scripts/build.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/react": "^18.0.0",
    "esbuild": "^0.28.0",
    "typescript": "^5.0.0"
  }
}
